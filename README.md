# Cline Security public MVDev fixture

This public repository contains synthetic-only infrastructure examples for the authorized Cline
Security GitHub App acceptance exercise. Nothing here is deployed, and no value represents a
client, employer, credential, or real finding.

The default branch is intentionally empty of analyzable infrastructure. Acceptance branches add
and then remediate a deterministic synthetic issue without merging the pull request.
